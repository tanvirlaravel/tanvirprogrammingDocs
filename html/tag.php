<?php include_once '../inc/header.php'; ?>
<div class="container">
  <?php include_once 'nav.php'; ?>

  <div class="row">
    <div class="col-4">
      <ul id="sidebar_links"></ul>
    </div>
    <div class="col-8">
      <h1 class="text-danger">Tag</h1>

      <h3>Defination</h3>
      <div>
      <p>HTML tags are elements that are used to define the structure and layout of an HTML document. They are surrounded by angle brackets (< and >) and are usually paired, with a start tag (e.g., &lt;p>) and an end tag (e.g., &lt;/p>). The content of an HTML element is placed between the start and end tags.</p>
       
        <p>There are many other HTML tags available, each with its own specific purpose and attributes. Some common tags include &lt;div> for dividing content into sections, &lt;form> for creating forms, and &lt;a> for creating links.</p>
       
       
        <hr />
        <hr />
      </div>

      <h3>Tag List</h3>
      <div>
     <pre>
      &lt;html>: defines the root element of an HTML document
      &lt;head>: contains metadata about the document, such as the title and character encoding
      &lt;title>: specifies the title of the document, which is displayed in the browser's title bar or tab
      &lt;body>: contains the actual content of the webpage
      &lt;h1> - &lt;h6>: defines headings of different levels
      &lt;p>: defines a paragraph
      &lt;br>: defines a line break
      &lt;hr>: defines a horizontal rule
      &lt;pre>: defines preformatted text
      &lt;ul>: defines an unordered list
      &lt;ol>: defines an ordered list
      &lt;li>: defines a list item
      &lt;img>: defines an image
      &lt;a>: defines a hyperlink
      &lt;div>: defines a division or section of the document
      &lt;span>: defines a section of text within a paragraph
      &lt;form>: defines a form for user input
      &lt;input>: defines an input field
      &lt;button>: defines a button
     </pre>
     <pre>
      &lt;!-->: defines a comment
      &lt;!DOCTYPE>: defines the document type
      &lt;a>: defines a hyperlink
      &lt;abbr>: defines an abbreviation or acronym
      &lt;address>: defines contact information for the author of a page
      &lt;area>: defines an area inside an image map
      &lt;article>: defines an article
      &lt;aside>: defines content aside from the main content of a page
      &lt;audio>: defines audio content
      &lt;b>: defines bold text
      &lt;base>: defines a base URL for all relative URLs in a page
      &lt;bdi>: defines text that should be isolated from its surrounding text
      &lt;bdo>: defines the direction of text display
      &lt;blockquote>: defines a long quotation
      &lt;body>: defines the main content of a page
      &lt;br>: defines a line break
      &lt;button>: defines a button
      &lt;canvas>: defines a canvas for drawing graphics
      &lt;caption>: defines a table caption
      &lt;cite>: defines the title of a work
      &lt;code>: defines a piece of computer code
      &lt;col>: defines a column in a table
      &lt;colgroup>: defines a group of columns in a table
      &lt;data>: defines a piece of data
      &lt;datalist>: defines a list of options for a &lt;input> element
      &lt;dd>: defines a definition in a definition list
      &lt;del>: defines deleted text
      &lt;details>: defines additional details that the user can view or hide
     </pre>
      <hr>
      <hr>
      </div>


    </div>
  </div>
</div>

<?php include_once '../inc/footer.php'; ?>
