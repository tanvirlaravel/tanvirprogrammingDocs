<?php include_once '../inc/header.php'; ?>
<div class="container">
  <?php include_once 'nav.php'; ?>

  <div class="row">
    <div class="col-4">
      <ul id="sidebar_links"></ul>
    </div>
    <div class="col-8">
      <h1 class="text-danger">attributes</h1>

      <h3>Defination</h3>
      <div>
    <p>HTML attributes are used to provide <code>additional information</code> or <code>specify the behavior</code> of an HTML element. </p>
    <p>They are added to the start tag of an element and are usually in the form of name-value pairs, with the name followed by an equals sign (=) and the value enclosed in quotes.</p>
    <pre>
    <a href="https://www.example.com">Visit our website</a>
    </pre>

    <p>In this example, the a element is an anchor element that creates a link to another webpage. The href attribute specifies the destination of the link.</p>
    <hr />
        <hr />
      </div>

      <h3>List</h3>
      <div>
    <pre>
    class: used to specify one or more class names for an element. This can be used in 
    conjunction with CSS to style elements with a particular class.

    id: used to specify a unique identifier for an element. This can be used in 
    conjunction with CSS or JavaScript to select or manipulate the element.

    src: used to specify the source of an image or other media file.
    
    alt: used to specify alternative text for an image. This text is displayed if the 
    image cannot be displayed for some reason.

    title: used to specify additional information about an element. This information is 
    usually displayed as a tooltip when the element is hovered over.

    accept: used to specify the types of files that a &lt;form> element can upload

    accept-charset: used to specify the character encodings that a &lt;form> element can submit

    accesskey: used to specify a shortcut key for an element

    action: used to specify the URL of a form-processing server

    align: used to specify the alignment of an element

    alt: used to specify alternative text for an image

    async: used to specify that a &lt;script> element should be executed asynchronously

    autocomplete: used to specify whether an &lt;input> element should have autocomplete enabled

    autofocus: used to specify that an element should be focused when the page loads

    autoplay: used to specify that a &lt;video> or &lt;audio> element should start playing as soon as it is loaded

    bgcolor: used to specify the background color of an element

    border: used to specify the width of an element's border

    challenge: used to specify that a &lt;keygen> element should include a challenge string in the generated key

    charset: used to specify the character encoding of a &lt;link> or &lt;style> element

    checked: used to specify that a &lt;input> element should be checked by default

    cite: used to specify the URL of a cited work

    class: used to specify one or more class names for an element

  
</pre>
       
        <hr />
        <hr />
      </div>
    
    
    </div>
  </div>
</div>

<?php include_once '../inc/footer.php'; ?>
